const ComplianceServiceRegistery = artifacts.require("ComplianceServiceRegistery");

contract("ComplianceServiceRegistery", accounts => {
  it("...should store the value 89.", async () => {
    const instance = await ComplianceServiceRegistery.deployed();

  });
});
